[package]
name = "mentat_sql"
version = "0.0.1"
workspace = ".."

[dependencies]
error-chain = "0.8.1"
ordered-float = "0.5"

[dependencies.rusqlite]
version = "0.12"
# System sqlite might be very old.
features = ["bundled", "limits"]

[dependencies.mentat_core]
path = "../core"
